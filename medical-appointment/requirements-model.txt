# Model dependencies for the verifier (the official requirements.txt is left
# untouched). torch is installed separately with the CUDA index in setup_env.sh.
transformers>=4.40
# Gemma 4 needs a much newer transformers than this floor; the served
# versions are pinned in idun/serving-env.lock.txt (written by idun/setup_llm.sh).
