# Drone flyby — status handover (Juan, 2026-09-17 16:18 CEST)

Branch: `waffles` (Pablo's `pablo/drone-flyby` merged in; code now lives under `drone-flyby/src/`, sample data under `drone-flyby/data/`). Nothing submitted for evaluation (one attempt only; a human presses Submit, never scripts or AI tools). The team API key is not in the repo and must never be.

## TL;DR

- Serving pipeline works end to end (FastAPI + YOLO + object memory + camera planner), camera moves are accepted by the live service (last validation: **0 errors**).
- The model only knows Helsinki, so the **live validation score is ~0 (0.0069)**. Locally on Helsinki it scores 0.855, which is memorisation, not skill.
- Root cause (checked on recorded validation frames): the **objects are the same 3D models** in both scenes, but the validation scene is a **city** (Copenhagen) while Helsinki is forest/lake. The model calls roads "hangar" and misclassifies helicopters. Fix = more varied training data, not a different model.
- We reconstructed the **full 4K validation frames** (249, ~84% native resolution) by recording several validation runs. They are unlabelled and too big for git: fetch them from the GitHub release with `training/fetch_data.sh` (see below).

## What is in the code

| File | What it does |
|---|---|
|  `src/solution.py` | Our predictor: YOLO + object memory + lag-safe camera planner. `src/api.py` serves it by default (`DRONE_STACK=solution`). |
| `src/core/`, `src/config.py`, `src/tests/` | Pablo's modular stack (detector / tracker / camera policy / pipeline), served with `DRONE_STACK=pipeline`. Merged in from `pablo/drone-flyby` on 2026-09-17; its detector is still template/Canny, so it scores 0.000 locally. |
|  `src/api.py` | Organiser template + logs 422s with the keys received (a 422 usually means the URL was pasted into another use case's form). |
| `training/make_dataset.py` | Cuts YOLO training crops from the 4K frames at levels 0/1/2 exactly like the evaluator (INTER_AREA). |
| `training/train_yolo.py` | Fine-tunes YOLO11s at 960 px (≈30 min / 40 epochs on an RTX 2060). |
| `training/extract_sprites.py` | GrabCut cut-outs of every ground-truth object -> `sprites/` + review sheets. Flags `CUT` (frame edge) and `SUSPECT` (odd fill ratio). Keeps hand-drawn outlines. |
| `training/sprite_review/` | Local web tool (http://localhost:8765) to accept/reject/redraw sprite outlines. Saves `sprites/manual.json`. |
| `training/stitch_recording.py` | Stitches a recorded run into one ground mosaic (drift estimated by template matching). |
| `training/reconstruct_frames.py` | Rebuilds full 3840x2160 frames from several recorded runs (Level-0 run for registration, Level-1/2 views pasted on top). |

###  `src/solution.py` switches (environment variables)

| Variable | Default | Meaning |
|---|---|---|
| `DRONE_MODEL` | `runs/yolo11s_baseline/weights/best.pt` | YOLO weights |
| `DRONE_CAMERA` | `hybrid` | `hybrid` (planner), `sweep` (L1 snake), `l0` (always full view), `record` (no detection, data collection; scores 0) |
| `RECORD_LEVEL`, `RECORD_ROW_Y` | `2`, `1080` | record mode: L2 back-and-forth scan along a row, or `RECORD_LEVEL=0` for full views |
| `DRONE_MEMORY` | `1` | `0` = report only the current view's detections |
| `DRONE_RECORD_DIR` | `recordings/` | every received view is saved there; empty string disables |
| `H_L0_WEIGHT` etc. | see file | hybrid planner weights |

### How the pieces work

- **Memory**: detections become tracks in 4K source pixels, moved by their own estimated drift (~62–68 px/frame downwards in both scenes) and reported while the camera looks elsewhere. It doubles mAP (0.31 -> 0.61 with the sweep), because every frame is scored against the whole 4K frame.
- **Hybrid camera**: scores reachable views by time since last full view, stale grid cells, and uncertain/never-zoomed objects. Uses Level 0 about 1/3 of the time.
- **Camera lag (important)**: the live service applies a camera command ~1 frame late, so a request can still show the old view. The server remembers its pending command and only sends moves legal from both positions. Before this fix, validations had 9–12 refused moves; now 0. The local evaluator does NOT simulate this lag.
- **Sequence reset**: state resets when `frame_index == 0` or the frame number goes backwards (the local evaluator reuses one sequence id).

## Results so far

| Setup | Local Helsinki mAP | Live validation |
|---|---|---|
| Organiser baseline | 0.000 | – |
| YOLO11s + memory + sweep | 0.614 | – |
| YOLO11s + memory + hybrid | 0.855 | 0 (refused camera moves) -> **0.0069 after lag fix, 0 errors** |
| YOLO11s + always L0 | 0.965 (inflated: trained on those exact L0 images) | not tried |

Latency: model ~20 ms on the GPU; the cloudflared tunnel from Juan's home was the bottleneck (~30% frames skipped in a normal run, ~4% in record mode). Serving from a better-connected machine would help.

## Validation scene findings

- **Location**: Copenhagen — Kalvebod Fælled (shooting range at Selinevej 5) north-west across Kalvebodløbet into Sydhavn (marina, industrial harbour) to city blocks. Helsinki reference scene location not found yet. Scenes look like Nordic capitals; evaluation is plausibly another one.
- **Same 3D models** in both scenes (hangar, helicopter, small_plane, jet_plane, tanks, towers, launchers...), different backgrounds, several instances per class, grouped (e.g. hangar + 3 planes).
- The background imagery is 3D (roofs shift with parallax) — likely photorealistic 3D map tiles in a simulator ("GimbalCamera"). Google's tile terms don't allow ML training, so for our own backgrounds prefer open national orthophotos (DK/FI/SE/NO), ~0.2 m/px.
- Camera geometry estimate: 600 m altitude, ~0.21 m/px, ~68° horizontal FOV, nadir.

## Data not in git: fetch it from the GitHub release

Everything below except `.venv/` and `datasets/` is on the private release
[`drone-data-2026-09-17`](https://github.com/csongorszasz/nordic-ai-cup-2026-amigos/releases/tag/drone-data-2026-09-17).
From `drone-flyby/` (needs `gh auth login`):

```bash
bash training/fetch_data.sh model        # YOLO weights + sprites, 36 MB: enough to serve and review sprites
bash training/fetch_data.sh frames       # 249 rebuilt 4K validation frames, 854 MB   (not uploaded yet)
bash training/fetch_data.sh recordings   # 7 raw validation runs, 1.4 GB              (not uploaded yet)
```

**Only the model zip is on the release right now.** The frames and recordings were uploaded
in 14 MB parts (uploads over ~15 MB fail with HTTP 500 from Juan's home line) and the 154
part assets were then deleted as unusable. Juan has the local copies; they need one upload
of three single files from a better connection. The frames can also be rebuilt from the
recordings with `training/reconstruct_frames.py`.

It verifies checksums, unpacks into place, and never overwrites existing files unless `FORCE=1`.
`recordings/RUNS.md` describes each run. Private team data: don't share outside the team.

### Where each piece comes from

| Path | Size | How to get it |
|---|---|---|
| `.venv/` | 7.7 G | `uv venv --python 3.12 .venv` + `uv pip install -r requirements.txt ultralytics "torch==2.5.1" "torchvision==0.20.1" --index-url https://download.pytorch.org/whl/cu121 --extra-index-url https://pypi.org/simple --index-strategy unsafe-best-match` (the cu121 pin is for NVIDIA driver 535 / CUDA 12.2) |
| `datasets/helsinki_yolo/` | 466 M | `python training/make_dataset.py` |
| `runs/yolo11s_baseline/weights/best.pt` | 19 M | `python training/train_yolo.py` |
| `sprites/` | 21 M | `python training/extract_sprites.py` (~4 min) |
| `runs/.../best.pt`, `sprites/`, `recordings/` | 2.3 G | GitHub release above (`fetch_data.sh`). Recordings cannot be regenerated without new validation runs. |

## Next steps (in the order we planned)

1. **Label the rebuilt validation frames**: add a box-labelling mode to the sprite review tool; label each object once on its sharpest frame and propagate the box with the known drift. Gives validation sprites and an honest local test set (a different scene from training).
2. **Synthetic data**: paste sprites (Helsinki + validation, reviewed) with random rotation/scale/hue onto varied Nordic city + nature orthophotos, then crop at L0/L1/L2 with `make_dataset.py`-style code. Include roads, roofs, harbours (the current false-positive sources).
3. **Optional 3D renders**: Juan found a close TIE interceptor model for `spacecraft` on Sketchfab (download `.glb` into `models/<class>/`, keep URL + licence in `credits.txt`). A `pyrender`/`trimesh` renderer can turn models into sprites from any angle. Priority classes: mine_roller, hangar, medium_launcher, medium_plane.
4. **Retrain** on synthetic + Helsinki, evaluate on labelled validation frames, then validate live. Compare `hybrid` vs `l0` camera on the live set.

## Running a server for a validation

```bash
cd drone-flyby
DRONE_CAMERA=hybrid .venv/bin/python -m uvicorn api:app --app-dir src --host 0.0.0.0 --port 9056
cloudflared tunnel --no-autoupdate --url http://localhost:9056   # prints a https://*.trycloudflare.com URL
```

Submit `https://<tunnel>/predict` in the **drone** form (Verify first). Quick tunnels get a new URL on every restart. Juan's servers and tunnels stop when his session ends, so the URLs in earlier chats are dead.

Local checks: `python src/local_evaluator.py --url http://localhost:9056/predict` (add `--realtime` for the frame clock).
